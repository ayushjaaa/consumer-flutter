import 'package:flutter/material.dart';

class AppColors {
  // Bright green used for primary actions (matches screenshot)
  static const primary = Color(0xFF2BB32A);
  static const background = Color(0xFF0F2317);
  static const card = Color(0xFF132A1C);
  static const textPrimary = Colors.white;
  static const textSecondary = Colors.white70;
}
